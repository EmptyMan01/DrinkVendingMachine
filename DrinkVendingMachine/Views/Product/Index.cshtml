@*@model IEnumerable<DrinkVendingMachine.Models.Products>*@

@*@if (Model.Count() > 0)
    {*@
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Name Sprite
            </th>
            <th>
                Sprite
            </th>
            <th>
                Quantity
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td width="30%">@item.NameSprite</td>
                <td width="30%">@item.Sprite</td>
                <td width="30%">@item.Quantity</td>
            </tr>

        }
    </tbody>
</table>

@*}*@
<h4> RTR</h4>
<a asp-controller="Product" asp-action="Create"> Create</a>
<a asp-controller="Product" asp-action="All"> All</a>
@*<dd class="col-sm-10">
        <img src=@("~/Sprite/"+Model.NameSprite)" asp-append-version="true" width="250px" height="250px" />
    </dd>*@